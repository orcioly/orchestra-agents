# estado de runtime (gerado em ~/.local/state/orchestra-agents normalmente)
# e runtime dos agentes dentro de cada projeto (o team.json É versionável)
.orchestra/run/
*.session
server.log
.run/
run/
state/

# lixo comum
*.log
.DS_Store
__pycache__/
*.pyc

# contexto local do Claude (não versionar)
CLAUDE.md
CLAUDE.local.md
.claude

# Python bytecode cache (gerado ao importar merge_reviewer em testes)
__pycache__/
*.pyc

# knowledge graph do graphify (gerado; os hooks post-checkout/post-commit reconstroem)
graphify-out/
